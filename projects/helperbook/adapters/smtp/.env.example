# Production
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your_email@gmail.com
# SMTP_PASSWORD=your_app_password
# SMTP_TLS=true

# Development (MailCatcher via docker-compose)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_TLS=false

SMTP_FROM_EMAIL=noreply@helperbook.app
SMTP_FROM_NAME=Helperbook
EMAIL_SERVICE_URL=adapter://smtp
